import { makeStyles } from "@material-ui/core/styles";

export const useShopStyles = makeStyles(() => ({
  shop: {
    maxWidth: 1200,
    margin: "0 auto",
  },
  cartHolder: {
    height: "100%",
  },
}));
